apiVersion: v1
kind: Service
metadata:
  name: pool
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "pool"
    port: 8080
  selector:
    app: pool
