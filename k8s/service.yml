apiVersion: v1
kind: Service
metadata:
  name: sitt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sitt"
    port: 8080
  selector:
    app: sitt
