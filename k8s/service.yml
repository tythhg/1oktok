apiVersion: v1
kind: Service
metadata:
  name: outlook
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "outlook"
    port: 8080
  selector:
    app: outlook
