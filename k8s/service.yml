apiVersion: v1
kind: Service
metadata:
  name: search
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "search"
    port: 8080
  selector:
    app: search
