apiVersion: v1
kind: Service
metadata:
  name: start
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "start"
    port: 8080
  selector:
    app: start
